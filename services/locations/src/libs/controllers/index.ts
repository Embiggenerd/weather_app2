export const ping = require("./ping")

export const allLocations = require("./all")

export const locationsByUser = require("./byUser")

export const singleLocation = require("./single")

export const postLocation = require("./post")

export const putLocation = require("./put")

export const delLocation = require("./delete")